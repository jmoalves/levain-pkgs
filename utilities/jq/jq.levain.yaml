version: 

dependencies:

downloadUrl: https://github.com/stedolan/jq/releases/download/jq-

cmd.install:
    - copy ${downloadUrl}/jq-win64.exe ${baseDir}/jq.exe
    - addPath --permanent ${baseDir}

cmd.env:
    - addPath ${baseDir}
