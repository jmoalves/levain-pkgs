version: 4.9.1

dependencies:

downloadUrl: https://nodejs.org/dist/v4.9.1/node-v4.9.1-linux-x64.tar.xz

cmd.install:
    - extract ${downloadUrl} ${baseDir}
    - setEnv --permanent NODE_HOME ${baseDir}

cmd.env:
    - addPath ${baseDir}/bin
    - setEnv NODE_VERSION 4.9.1
