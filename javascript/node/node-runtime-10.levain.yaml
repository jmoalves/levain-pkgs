version: 10.24.1

dependencies:

downloadUrl: https://nodejs.org/dist/v10.24.1/node-v10.24.1-linux-x64.tar.xz

cmd.install:
    - extract ${downloadUrl} ${baseDir}
    - setEnv --permanent NODE_HOME ${baseDir}

cmd.env:
    - addPath ${baseDir}/bin
    - setEnv NODE_VERSION 10.24.1
