version: 20.15.0

dependencies:

downloadUrl: https://nodejs.org/dist/v20.15.0/node-v20.15.0-win-x64.zip

cmd.install:
    - extract --strip ${downloadUrl} ${baseDir}
    - setEnv --permanent NODE_HOME ${baseDir}

cmd.env:
    - addPath --permanent ${baseDir}
    - setEnv NODE_VERSION 20.15.0
