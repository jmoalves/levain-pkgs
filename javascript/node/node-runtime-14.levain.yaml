version: 14.21.3

dependencies:

downloadUrl: https://nodejs.org/dist/v14.21.3/node-v14.21.3-linux-x64.tar.xz

cmd.install:
    - extract ${downloadUrl} ${baseDir}
    - setEnv --permanent NODE_HOME ${baseDir}

cmd.env:
    - addPath ${baseDir}/bin
    - setEnv NODE_VERSION 14.21.3
