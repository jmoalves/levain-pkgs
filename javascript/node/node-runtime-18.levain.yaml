version: 18.20.3

dependencies:

downloadUrl: https://nodejs.org/dist/v18.20.3/node-v18.20.3-linux-x64.tar.xz

cmd.install:
    - extract ${downloadUrl} ${baseDir}
    - setEnv --permanent NODE_HOME ${baseDir}

cmd.env:
    - addPath ${baseDir}/bin
    - setEnv NODE_VERSION 18.20.3
