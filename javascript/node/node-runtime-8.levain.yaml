version: 8.17.0

dependencies:

downloadUrl: https://nodejs.org/dist/v8.17.0/node-v8.17.0-linux-x64.tar.xz

cmd.install:
    - extract ${downloadUrl} ${baseDir}
    - setEnv --permanent NODE_HOME ${baseDir}

cmd.env:
    - addPath ${baseDir}/bin
    - setEnv NODE_VERSION 8.17.0
