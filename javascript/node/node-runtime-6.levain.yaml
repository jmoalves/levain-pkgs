version: 6.17.1

dependencies:

downloadUrl: https://nodejs.org/dist/v6.17.1/node-v6.17.1-linux-x64.tar.xz

cmd.install:
    - extract ${downloadUrl} ${baseDir}
    - setEnv --permanent NODE_HOME ${baseDir}

cmd.env:
    - addPath ${baseDir}/bin
    - setEnv NODE_VERSION 6.17.1
